/**
 * @deprecated GoHighLevel SMS removed — use sms.ts (Twilio) instead.
 * Kept as a shim so any stale imports continue working.
 */
export { formatPhoneE164, sendSms, sendSmsViaGhl } from "./sms.ts";
